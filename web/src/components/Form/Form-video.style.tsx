import styled from "@emotion/styled";
import {
  Dialog,
  Typography
} from "@mui/material";


export const DialogVideo = styled(Dialog)(() => ({
  "& .MuiPaper-root": {
    background: "#1F1F1F",
  },
}));

export const FormLabel = styled(Typography)(() => ({
  color: "white",
}));


